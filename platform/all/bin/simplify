#!/usr/bin/env python3

# simplify
# Simplifies fractions when you're too lazy to!

import sys
from fractions import Fraction

if('/' in sys.argv[1]):
	frac = Fraction(sys.argv[1])
else:
	frac = Fraction(int(sys.argv[1]), int(sys.argv[2]))

print(str(frac))